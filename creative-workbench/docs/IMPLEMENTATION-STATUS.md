# 实现与验收记录

## 2026-09-06 · 简化掌柜复盘

- 复盘改为单栏，只保留成品照、评分、家庭记忆/下次改进评论，以及完成与保存草稿按钮。删除库存/临时用料说明、本机保存提示、实际剩余清单、另用批次、重新预填按钮和额外消耗确认勾选；打分后即可完成。
- 新增仅接收照片/评分/评论的 `finishCooking`，在同一 IndexedDB 事务内按开做时计划处理库存，再复用完成验证和幂等保护。不使用旧草稿里的消耗清单，不将自动配方记账伪装成用户实测消耗；以 `consumptionMode: recipe-plan` 区分。
- 所有新会话固定原库存分配，临时用料不入库、不重复扣量，后续新增批次不参与；同批次补货只扣原分配量。原批次被删除、换单位/身份或变成模糊量则保持原样，不因已删除的“移除批次”入口卡住整餐。现有量不足时降至零，不变负数。
- 旧会话缺少计划时只从原库存快照、保存份量和开做日期回退；已有照片、评分、评论草稿继续恢复。空计划不会回退至当前库存。已完成记录不迁移、不重扣。
- 148 项自动化测试、TypeScript 与业务 lint 通过，覆盖精简表单源码约束、全库存/全临时/混合用料、后来补货、异常批次、旧草稿和旧快照、跨日、精度、无效反馈原子回滚与两个连接重复完成。验证不等于真实浏览器点击或实际烹饪验收。
- 沿用当前本地 HTML 文件及既有数据库入口；照片/语音连接、菜谱/美术和其他分支不改，不发布或推送。

## 2026-09-06 · 本餐确认拥有，不新增库存

- 菜谱用料不足时显示红色“确认拥有”，点击变成绿色“已拥有”，可再次点击取消；精确库存已足量的条目直接显示已拥有。每项备齐后，仍须勾选食材/到期信息/过敏原/厨具核对才能开始做，保留当前餐互斥及做法版本检查。
- 确认的是本餐差额，不产生库存或候选：需 2 个鸡蛋、已有 1 个，另外 1 个只在本餐使用。开做前草稿只在页面；关闭/重开/换菜/切厨房会清空，相关库存、到期状态和做法变化会使旧确认失效。开始时事务重新校验。
- 会话保存临时差额与原批次用量，刷新/保存草稿可继续。复盘只投射开做时分配的库存，不自动挑选后来新增批次；同批次补货只扣原分配量。临时食材不重复扣减，全部临时备齐时无需扣冰箱库存。旧会话没有新字段时沿用原逻辑，不迁移已有库存。
- 默认复盘与完成事务都保留库存精度；覆盖小数库存和途中补货，避免舍入导致额度冲突。原批次消失/单位改变会要求核对，不会换扣其他批次；重复完成和旧 revision 仍受原子/幂等保护。
- 138 项自动化测试通过，包括 70 道全临时用料闭环、部分缺料、途中补货、过期/模糊单位、跨厨房/版本/到期变化、IndexedDB 重开及回滚、红绿按钮静态渲染。TypeScript 与业务 lint 通过；不将静态渲染或模拟存储测试当作真实浏览器点击验收。
- 本轮保持照片/语音连接及原材料美术不变；只更新本分支本地 HTML，不发布、不推送或替换原运行工作目录。

## 2026-09-06 · 精简开做确认

- 按用户截图删除整个人工审校区域：说明、姓名输入、第二个勾选与记录按钮；去掉详情和目录里的“待人工审校/审校人”标签及死状态、样式。
- 仅保留食材、到期信息、过敏原与厨具的本餐确认勾选；未勾选时按钮不可用，点击处理也会拒绝。勾选绑定厨房、菜品与做法版本，重新打开或收到新做法需重新确认。样例演练仍保留原本的样例确认文案。
- 去掉 `startCooking` 对具名人工审校的阻拦；库存足量、过期排除、菜谱版本一致、当前餐互斥、实际完成后才扣库存的检查不变。旧 reviews 数据和旧会话不迁移、不删除，也不会自动伪造新的审校记录。
- 124 项测试、TypeScript、业务 lint、Web / 独立 HTML 构建通过；独立 HTML 验证审校面板文字全部移除、保留确认文案，71 个美术素材内嵌检查通过。新增 UI 源码回归，并覆盖 70 道预设菜和 WorkBuddy 做法在无新具名审校时正常开做。真实浏览器点击不由源码与内存测试代替。

以下为此前版本的历史记录；旧版“必须记录人工审校”的流程已被上方替代。

## 2026-09-06 · 70 道预设家常做法与中文来源

- 独立 `codex/baiwei-70` 分支，在百味图美术提交 `bfa8797` 上继续；不修改另一分支的原材料美术，不替换原运行工作目录或库存，不新增模型、依赖或外部 API。
- `lib/home-recipes.json` 包含全部 70 道、456 个详细步骤、71 条中文原文来源（71 个不同链接），版本 `2026-09-06-home.1`。逐道注明基础人数、产出、材料与调料总量；逐步注明所用份量、火候、分钟数、操作和完成判断。来源作者、支持范围、改编说明在菜品详情与 `CHINESE-RECIPE-SOURCES.md` 展示。
- 分组检索并打开中文原作者页面，重新编写定量家常版本；跨组复核修正汤水交接、解冻前置和遗漏的烧开时间。引用不是原方逐字复制、历史故事背书或实做证明；全部保留待有经验的人核对的边界。数字分钟数是估算，含明确列出的等待，非原作者精确计时。
- 推荐与百味图直接打开本地做法，不调用 WorkBuddy；跟做页显示当前步骤的用料、估时、火候与判断，可按本步时间启动提醒，不自动跳步。WorkBuddy 另写做法保留在折叠的可选区，仅显式请求时生成；人数随基础配方，不能重复乘总用料，也不套用预设计时。
- 旧会话优先读取快照；无快照且旧版本的三道菜读取原封保留的 `legacyRecipes`，不改旧用量/来源/历史。新开餐使用新配方；旧审校不会自动批准新版。`servings` 仍为内部份数倍率，界面用基础人数乘倍率显示实际人数，已有数据库结构不变。
- 新增精确食材别名及干湿/生熟/去骨状态区分；旧 `other` 名称只读匹配，不整库迁移。语音中鱼豆腐、毛豆腐等完整词不再拆为普通豆腐；现有库存全名优先，词表正则转义，避免错误盘点另一种食材。语音转写运行环境和照片连接不变。
- **123 项自动化测试通过**。覆盖全部 70 道结构、用料引用、中文来源、服务端静态渲染、完整跟做/扣量/幂等点亮/IndexedDB 模拟重开、旧配方快照、食材状态不混用、自动化 HTTP 交接及语音加工品身份。独立审查复现的豆腐别名问题已修复并增加回归测试。
- TypeScript、业务 lint、新组件 lint、Web 构建、独立 HTML 构建通过。独立 HTML 约 **19.34 MB**，全部 71 个原游戏图像字节哈希及内嵌检查通过；构建的大包警告主要来自离线内嵌插画。中文来源索引随 HTML 一并生成。
- **未实际烹饪 70 道、未做真实浏览器/手机交互或 WorkBuddy 模型调用验收，未发布、推送或合并主分支。** 自动化演练不代表这些验收。旧 ZIP 和私人运行目录保持原样。

以下为此前版本的历史记录，不代表当前菜谱数量或入口行为。

## 2026-09-06 · 百味图 70 道成品菜美术与故事

- 独立 `codex/baiwei-70` 分支，基于 main `4c82a6e`；未改动或合并原材料插画分支。新增素材目录、图鉴内容与展示组件，仅在页面百味图位置挂载；库存卡片 / 录入 / 菜谱步骤 / WorkBuddy 协议不变。
- 来源固定为 Chinese-Cuisine `ae825f252ae982ec05d359edff44e226cef2fca2`。70 条原文故事、70 张原始菜品 WebP、1 张通用盘子，全部核对 Git blob SHA-1。21 道自带容器（其中 14 道汤）、49 道叠盘，照搬原作几何比例；白米饭保持独立缩放。
- 百味图显示全部 70 道，未做过的插画完整灰度；点亮由本厨房 `completed` 会话推导，不新增 unlocked 标志，不生成假库存或假完成记录。重复制作累积历史，最近一次按时间排序；样例 / 真实隔离。详情保留原作插画、故事以及用户每次成品照 / 评分 / 日期 / 食忆；历史入口继续读取当次菜谱快照。
- 图鉴与 Recipe 类型分离：原有三道现实菜谱均在 70 图鉴内，原 ID、做法版本和审校记录不变。其余 67 道仅图文，明确提示现实做法待整理，不调用原有只支持三道菜的 WorkBuddy 流程，不冒充可以跟做。原作故事未逐条史实考证，页面明确区分文学故事与实用做法。
- 113 项自动化测试通过，新增目录 / 素材完整性、容器组合、70 灰态服务端渲染、完成点亮、重复制作、旧会话和 IndexedDB 重开隔离检查。自动测试和响应检查不代表真实浏览器交互、手机外观或 WorkBuddy 实机验收。
- Web 产物使用独立图片 URL 和延迟加载；独立 HTML 内嵌 71 个完整图片字节，无额外图片请求。保留旧 ZIP，不覆盖原本机桥接工作目录、用户库存或历史记录。本轮未新增依赖或模型。

以下为此前版本的历史记录。

## 0.4.2 · 自己的 WorkBuddy 生成做菜步骤

2026-09-06。沿用已经验收的照片本机交接，不接外部 AI API、不下载/启动本地菜谱模型，不读取账号凭据。网页准备所选菜的名字和基础 1 人份用料；用户在自己的 WorkBuddy 对话说“读取厨房任务，生成做菜步骤”，新版 Skill 读取任务并提交生成结果，页面自动接收。

- 独立做法队列，与照片票据/候选分离；复用本机端口、workspaceId、来源/客户端/Agent 令牌检查、串行持久化。只传一道菜的用料，不向 Agent 开放整库读取。
- 严格校验菜名/ID/版本、步骤数量与长度；重复交回幂等，跨厨房/过期/取消/重置后的结果不能重新入库。写入 IndexedDB 成功后才回执；服务重启保留未完成任务。
- 返回做法仅为草稿，不自动审校、不开始做菜、不扣库存。审校/勾选绑定当前显示版本，事务再次校验版本，防止多标签页确认未看过的新步骤。开餐固定做法快照，详情、逐步跟做及百味图导出都保留本餐版本，旧会话仍用原做法。
- 本次不扩展推荐目录，仍为三道基础菜；真实 WorkBuddy 模型输出和浏览器交互尚需用户验收。页面不能主动唤醒 WorkBuddy，不能把传输自动回传称作全自动生成。
- 104 项自动化测试通过，涵盖真实本地 HTTP、Skill CLI、重启/幂等、失败不回执、取消后迟到响应、陈旧版本审校、固定快照和复盘导出。步骤为明确标注的测试夹具，IndexedDB 使用 fake-indexeddb；不是操作用户账号或真实模型生成的证据。TypeScript / 业务 lint 已通过。
- Web 构建、独立 HTML 构建、16 文件 ZIP 逐项压缩往返校验和 Skill 结构校验通过。未新增模型、依赖或初始化脚本，旧语音运行环境不动；未联网部署、未操作浏览器或用户 WorkBuddy 账号。

## 0.4.1 · 第一批厨房 UX 精简

2026-09-06。用户反馈主流程已跑通，本轮仅按其指定内容优化录入、推荐设置与盘点表单。

- 首页照片按钮改为“拍照录入食材”；我的厨房改为“拍照识别”。手动补充使用与拍照相同的边框按钮，调料清单改为文字入口，排列为语音 / 拍照 / 手动 / 调料。删除库存工具栏下的浏览器存储说明，保留备份功能和文档中的存储边界。
- 删除“今天这一餐”用餐设置，推荐只按已确认库存覆盖与临期情况，不再读取历史保存的人数、时间、厨具、过敏和忌口设置。默认厨具齐备，新开餐用基础 1 人份；已进行中的会话仍保留原份量。食材数量校验、过期排除、菜谱审校与原安全提示不变。
- 照片候选与语音预览统一保留数量形式、数量 / 单位、到期日期。名称只在条目标题展示，删除名称 / 对应食材和批次选择器。语音识别名称有误仍可改转写文字后重识别，错误照片候选可拒绝再手动录入。
- 唯一同名库存自动关联；点击库存卡片“校准余量”会内部绑定对应 id / revision 并保留日期，刷新后仍可校准。同名多份库存不猜测、不合并，提示从各原库存卡片分别修改。补货新增独立记录，盘点不累加，未知内置类别按原名称单独保存。
- 简化确认仍检查匹配库存快照，防止跨窗口重复建批或覆盖陈旧记录；过期记录也保留日期，不会被识别操作自动续期。未识别食材、调料记录、照片连接、本机语音模型与数据库标识不变。
- 94 项自动化测试通过（含新增盘点匹配、过期、多份同名、并发、重开数据库、旧偏好忽略及源码 UI 约束），TypeScript / 业务 lint 通过。Web 构建通过；本轮未做浏览器点击或麦克风实测，源码断言与 fake-indexeddb 测试不等于这些实机验收。

以下 0.4 的名称 / 批次编辑说明已由本节精简流程取代；保留历史记录。

## 0.4 · 网页语音录入到库存

2026-09-06。用户明确允许 WorkBuddy 或其他 AI，不再以开放平台申请作为语音实现前置。当前采用本机 faster-whisper / Whisper base CPU int8 真实转写，食材与数量由显式词典 / 规则提取，不冒充大语言模型理解任意表达。照片仍保留原 WorkBuddy 连接。

### 已实现

- 「语音录入」独立主入口，网页麦克风授权、开始、结束识别、取消；60 秒 / 3 MB 限制，关闭或切换厨房释放麦克风、取消请求并忽略过时响应。
- 同源本机 `/voice/transcribe`，固定 Python 子进程，只允许音频、限定体积 / 解码时长 / 120 秒超时 / 单并发；无 shell 拼接。音频不落盘，不调用外部 AI、不触碰账号凭据。首次初始化下载依赖与模型，运行时强制本地文件。
- 自动显示文字和食材清单，可改字后重新解析、取消条目、改数量 / 对应食材 / 批次。普通话数词、半份、公斤 / 斤换克、包装不猜内含量；否定 / 购买计划不加库存，范围 / 改口 / 重复数量保守待核对。
- 单次确认在现有四表 IndexedDB 事务提交全部所选条目。预览不改库，事务失败可重试且不污染 UI 草稿；确认幂等。盘点保留旧到期日，无量时保留明确旧数量；库存版本变化阻止陈旧草稿重复建批。
- 原 workspaceId、来源、数据库结构、个人照片库存、调料清单均保持；不把 JSON 导入加回来。

### 验证证据与边界

- 83 项自动化测试通过，包括中文解析、否定 / 模糊数量、原子回滚、重复确认、并发盘点、旧数量保留、补货合并、录音取消 / 迟到权限 / 超时释放，以及音频 HTTP 的跨站与体积限制。
- `scripts/verify-voice-live.ts` 使用 macOS 合成普通话“冰箱里有两个番茄，一盒鸡蛋，还有半斤猪肉”，经真实语音模型和真实 HTTP，识别出 2 个番茄、1 盒鸡蛋、250 克猪肉。之后真实业务确认函数写入 fake-indexeddb，关闭重开仍为 3 个批次；不触碰用户库存。最近该次转写约 0.93 秒，不作为所有音频的延迟保证。
- 同一合成音频转换成 WebM/Opus 和 MP4/AAC 后也通过真实 HTTP + 模型识别；损坏音频被拒绝。TypeScript、业务 lint、Web 构建、独立 HTML / 14 文件 ZIP 压缩往返校验通过。真实浏览器按钮 / 用户麦克风、各口音、手机和最终 WorkBuddy 承载环境没有实机验收，不能把合成音频或 fake-indexeddb 称为这些验收。
- 当前开发电脑的 `.kitchen-voice` 已初始化。新电脑仍需初始化和兼容的本机运行环境；这是本地工作版，不是已公网发布、任意手机零配置的语音服务。有限词典可能漏掉生僻食材，页面提示核对和补充。
- v0.4 干净 ZIP 包含初始化入口和语音脚本，不包含模型、Python 环境、运行目录或私人数据。保留 v0.1–v0.3 历史包。

以下为历史记录，0.3 中“网页语音未完成”及开放平台讨论已由本节的新实现取代。

---

## 0.3 后续 UX 修正 · 去掉技术导入入口

- 用户明确：厨房网页点麦克风，自动交给 WorkBuddy 识别；不接受对话听写作为该功能的替代。
- 根因：v0.2/v0.3 保留了用户可见的备用 JSON 导入；所谓“听写”只有对话说明，未实现网页语音转写或网页唤起 WorkBuddy。原有桥接仅负责 WorkBuddy → 页面候选。
- 已移除两个 JSON 导入入口、整个文件 / 粘贴表单、对应状态与图标；保留内部候选校验、原生 WorkBuddy 照片连接、手动补充与私人备份导出。存储代码、workspaceId、原用户库存不变。
- 移除首页“拍照 / 听写”的合并宣传，在连接说明中标明网页麦克风尚未接通；未添加假录音按钮或模拟 AI 成功状态。
- 用户截图含“导入食材 JSON”，但本轮修改前 43117 返回的是带“备用：导入 JSON”和“调料清单”的 v0.3 内容。截图页面不是当时服务器返回的最新版；未检查用户浏览器，无法进一步断定旧标签页、旧文件或其他入口。

### 网页语音的真实前置条件

官方 [第三方应用接入](https://open.workbuddy.cn/docs/third-party-app) 要求创建应用、配置回调 / scopes、审核并启用，然后用户 OAuth 授权；应用密钥和刷新令牌不能放在网页。用户已确认尚未创建开放平台应用。

官方 [Open API](https://open.workbuddy.cn/docs/openapi) 提供本地助理状态查询、发送文本及回复查询。仍需验证本地助理在线，以及消息是否能进入正确的厨房任务 / 工作目录。普通已登录账号不能替代第三方应用接入。当前未申请应用、未使用账号凭据、未调用任务 API，也未擅自搭建其他模型代理。

补查发现官方 [MCP Apps 接入](https://cloud.tencent.com/document/product/1831/137044) 还提供对话内 HTML 组件及 `app.sendMessage` 回写 / 触发 Agent 的能力，本地 MCP 配置示例不要求 Open API OAuth。但文档适用范围明确为 CodeBuddy Code 的 `--serve` Web UI 和 IDE 内嵌 Web UI，未证实当前 WorkBuddy 个人工作台 HTML 预览器支持。该路线需改为宿主加载的 MCP 组件，并验证宿主与麦克风权限；不能直接套用到现在的独立 localhost 页面。因此“所有实现都必须申请开放平台”不是已验证结论。

新增源码级 UX 回归检查：修正前两项失败、一项通过；修正后全部通过。该检查不等于浏览器或麦克风实测。重新构建 release HTML 供当前本地入口使用，历史 ZIP 保留原样，不作为本轮更新包。

本轮完整检查：64 / 64 自动化测试通过，TypeScript、业务 lint、Web 构建、独立 HTML 构建通过。没有实际录音或 WorkBuddy 自动调用验收；这次只完成技术入口清理，网页语音功能未完成。

---

## 0.3 · 新用户调料清单

用户本轮明确反馈 WorkBuddy 照片验收完成。该结论记录为**用户实机验收通过**，不冒充本轮代理又执行过 WorkBuddy 视觉测试。本轮保留该输入链路，只改调料初始化与必要的推荐展示。

### 行为与兼容性

- 首次空白真实厨房直接呈现清单；已有厨房提供非阻断入口，样例不强制 onboarding。
- 24 种调料：常用 8 项、酱料 / 风味油 6 项、香料 10 项。未知库存不预选，常用组可由用户主动一键勾选，更多项折叠显示。
- 明确确认才批量入库，跳过只记录设置状态；选择中或关闭不写库存。其他食材继续 WorkBuddy 照片 / 核对听写，手动为备用。
- 新增“有，数量待确认”数量档，不猜重量、容量、保质期或用量足够。该档位属于本地用户确认，未要求 WorkBuddy 改变输出协议。
- 已有未过期、未耗尽批次保持原样；过期 / 用完批次保留，确认后新增记录。未勾选不会删库存。
- 批次、setup 完成标记与幂等回执在同一 IndexedDB 事务提交；重复点击 / 重试 / 两连接确认不重复加同种有效调料。real / demo 独立，样例 reset 清空自己的 setup 标记。
- 推荐发现区分“没备调料”与“已备但用量未知”；后者可展示待核对菜谱，`enough`、现实审校与开做门槛保持不变，不虚构消费。
- 原存储名、workspaceId 和四表布局不变；未执行迁移、清空或重建私人库存。

### 本轮验证

新增 15 项自动化覆盖：清单静态首屏不预选 / 不写库、目录 ID、明确确认、跳过持久化、旧厨房兼容、非法输入和跨厨房拒绝、已有批次保护、过期 / 用完、重试、并发、数据库失败原子回滚、样例隔离，以及未知量的推荐 / 开做 / 扣减边界。测试使用固定 fixture、服务端组件渲染和 fake-indexeddb；不称为浏览器点击验收。

- 全量 `npm test`：61 / 61（旧闭环及交接 46 项 + 新增 15 项）。
- TypeScript、业务 lint（包含新增组件）、Web 生产构建通过。
- v0.3 独立 HTML 与完整 ZIP 使用同一源码；保留 v0.1 / v0.2 历史包，不包含私人运行目录。

本轮未进行真实浏览器交互测试。用户可刷新原入口，在“我的厨房 → 调料清单”直接验收，不必清空已经录入的照片库存。

---

## 0.2 · WorkBuddy 对话输入与本地自动交接

2026-09-05。用户确认：照片从 WorkBuddy 对话上传；未来复用者也使用自己的 WorkBuddy 对话。优先完成自用工作台，不做「做同款」、不申请开放平台、不改 Jacky 项目。此节取代下方 0.1 关于“正常流程需手动 JSON”的旧状态，旧记录保留为历史证据。

### 本次实现

- 原生对话照片/核对文字 → 中华食肆 Skill 提取 → 本地 Node 脚本提交 → 网页自动显示候选 → 用户人工确认。
- 去掉主流程的大段 prompt、复制说明、手动搬运 JSON；保留明确标注的手动备用入口。
- 仅本机 127.0.0.1:43117，同源请求、Host/Origin 校验、网页请求头、独立 Agent 令牌；令牌只在私有运行目录，网页/状态输出/ZIP 不含它。
- 页面创建 ticket 固定真实/样例、盘点/补货、投递身份；脚本不允许自行创建页面 ticket，不读取或写入正式库存。
- 候选队列写盘、同任务重试幂等、不同内容冲突拒绝、过期/取消失效。页面事务成功才回执；重启、ack 丢失和跨标签重复接收不加库存。
- 清空采用只读预检查 → IndexedDB 清空与忽略标记原子提交 → 取消指定投递。数据库清空失败保留队列，旧回包不能复活；后续新 ticket 不受影响。
- 新入口按工作台 workspaceId 分隔 IndexedDB；旧 file:// 和开发端口的数据仍在原来源，不自动迁移。仅浏览器本地投递身份使用 localStorage，未改库存唯一真源。
- 完整包增加 macOS 启动文件和 Node.js 22+ 无依赖脚本。停止连接不删除库存或待接收队列，重建 HTML 后需停止旧连接再启动新版。

### 本轮验证

| 检查 | 结果 / 范围 |
| --- | --- |
| npm test | 46 / 46；保留 29 项原闭环测试，新增 17 项交接测试 |
| 本地 HTTP + 文件队列 + fake-indexeddb | 排队 → 候选提交 → 回执 → 人工确认；未确认时库存为零；数据库失败不回执 |
| 队列重启与重放 | 工作台身份、ticket 与结果保留；已确认/拒绝候选不被重建；不同内容复用 ID 拒绝 |
| Skill 脚本真实子进程 | start 复用连接、status 取 ticket、submit 读中文含空格路径文件、查询 staged 回执；不输出令牌 |
| HTTP 访问边界 | 非同源/null Origin、伪造 Host、无令牌提交、无客户端标识、静态读取运行文件均拒绝 |
| 清空失败与校验一致性 | 数据库失败前后候选可恢复；取消旧投递不影响新任务；warnings 300 字与数量上界一致 |
| TypeScript / lint:app / Web 构建 | 通过；全仓脚手架旧 lint 状态仍按下方记录，不作无关修改 |
| Skill quick_validate.py | 通过；只证明文件结构，不代表 WorkBuddy 已安装 |
| 完整包 | 自包含 HTML 约 0.78 MB；v0.2 ZIP 明确列出 10 个文件，UTF-8 文件名及压缩往返逐字节验证通过 |
| 实际完整包启动 | release 目录启动成功，重复 start 复用同一连接；127.0.0.1:43117 返回 HTTP 200，未据此宣称浏览器交互已验收 |

### 仍需 WorkBuddy 实机验收

本轮没有操作用户的 WorkBuddy 账号或安装 Skill，没有用真实照片触发 WorkBuddy 模型，也没有做真实浏览器按钮/存储测试。上面的测试使用清楚标注的文字提取 fixture、真实 Node HTTP、fake-indexeddb，不能称为 WorkBuddy 视觉端到端已验证。

验收步骤：加载新版 Skill → 启动完整包 → 页面准备接收 → WorkBuddy 上传照片 → Skill 自动提交 → 页面候选出现 → 核对并确认一项 → 重开相同入口核对库存。需要用户授权 WorkBuddy 在指定目录运行脚本与写候选文件。若内嵌 iframe 受保护无法加载，使用本机浏览器打开相同入口，不放宽跨站访问规则。

所有个人运行数据（.kitchen-bridge）排除在 Git 和显式 ZIP 清单之外；Skill 写出的 extraction 文件留在本机供失败恢复，不自动删除。版本仍为本地工作版，未部署公网，未打包或展示私人照片/候选。

---

## 0.1 历史验收记录

2026-09-05 · 中华食肆创意工作台 · 本地工作版 0.1

## 当前交付，不扩大范围

交付中华食肆 HTML、最小中华食肆 Skill、三道家常菜来源台账与测试。未修改 Jacky 的生产工作台、原游戏仓库业务代码或 WorkBuddy 的 gate-test 文件。没有部署到公网，也没有安装 / 发布模板。

当前优先级是可操作的产品闭环，并不表示整个 v0.6 spec 或比赛提交物已完成。

## 已实现

- 今日一餐、我的厨房、做菜模式、百味图四个工作区。
- 结构化手动输入、显式 JSON 导入、逐候选确认 / 拒绝、盘点校准 / 补货。
- 浏览器原生 IndexedDB 唯一真源；四个 store 的同一事务，real / demo 隔离。
- 精确数量、数量档、到期信息、原料/调味/饮用水；不默认油盐存在，不默算盒装枚数或克数。
- 稳定规则推荐，最多三道；缺料、过敏原、青椒忌口、厨具、时长过滤。
- 来源草稿与个人实际审校记录分离；现实跟做需人工审校，样例可演练。
- 单人 / 两人用量一致缩放；分步进度、暂停与恢复、持久化提醒截止时间。
- 可选成品照、本地压缩、自评分、家庭记忆、复盘草稿。
- 实际剩余量显式确认；重复完成不多扣，冲突提示重新确认。
- 百味图按完成会话聚合；单条 HTML 导出不包含库存，用户文字转义。
- 完整状态导出，但整库备份恢复 UI 尚未实现。
- 同源代码生成开发网页与独立 HTML；没有维护两套厨房业务实现。

## 本次已跑的检查

| 检查 | 结果 | 能证明什么 |
| --- | --- | --- |
| npm test | 29 / 29 通过 | 规则与事务自动化，详见下表；不等于真实浏览器磁盘测试 |
| npm run typecheck | 通过 | 当前 TypeScript 类型检查 |
| npm run lint:app | 通过 | 本次业务代码静态规则 |
| npm run build | 通过 | Vinext Web 生产构建；路由分类提示为 Unknown，不是发布成功 |
| npm run build:html | 通过，约 0.77 MB | 自包含单 HTML 构建；单包超过 500 KB 的提示因单文件交付保留 |
| 单 HTML 模块解析 | 通过 | 恰好一个内嵌模块，JS 语法可解析，游戏插画以 data URL 内嵌 |
| 本地 HTTP 编译检查 | HTTP 200，localhost:5177 | 开发路由可编译返回；没有据此宣称按钮都完成了浏览器实机验收 |
| Skill quick_validate.py | 通过 | Skill 文件结构和元数据有效；不代表 WorkBuddy 加载实测 |
| npm run package:local | 通过 | ZIP 明确标记 UTF-8 中文文件名，7 个文件压缩后逐字节往返一致 |

全仓 npm run lint 尚有 Sites 脚手架原有 components/ui 与 hooks 的静态问题。未修改供应组件或全局关闭规则来掩盖它们，另提供 lint:app 精确覆盖本次业务代码。独立图片使用 img 是本地单文件与用户上传图片的必要选择，已在文件顶部说明局部规则例外。

### 自动化关键证据

- 连续确认一个包中的多个候选；重复导入 / 确认不加库存；拒绝后重传仍拒绝。
- 元数据矛盾、重复候选 ID、负数、伪造 rawMention 均拒绝。
- “剩饭”数量保持未知；“瓶”保留警告不换算；文件 confirmed 标记不生效。
- 盘点覆盖明确批次、未拍到食材保留；补货只合并相同精确单位和到期信息。
- 固定日期重复推荐十次一致；鸡蛋过敏、缺少锅具、忌口和时间硬过滤。
- 过期批次不计入；另一有效批次仍可用；盒装和模糊数量不假装足量。
- 未人工审校的真实菜谱不能开始；两人步骤中的明确毫升数与食材同步变化。
- 跟做不扣库存；完成才扣；重复完成保持一次；同菜两次只增加历史。
- 完成时 revision 冲突、负数、超量不局部写入。
- fake-indexeddb 连接关闭后重开可读回会话 / 库存；real / demo 分区与重置不互相污染。
- 在 inventory 已排队 put、sessions 即将 put 时注入异常，四个 store 全部回滚。
- 两个连接同时完成同一餐只扣一次。
- 百味图导出转义 script 字样、拒绝未完成会话，不带私人 inventorySnapshot。

## 还不能宣称通过的验收

1. WorkBuddy 指定版本真实加载 Skill，照片 / 已确认文字 → JSON → HTML 确认 → 关闭重开读回。
2. file://、WorkBuddy 内嵌、手机三种承载环境的独立存储、离线重开、权限拒绝和设备兼容性。
3. 真正浏览器里的按钮、触屏、键盘和窄屏 QA。本次没有把 HTTP 200 或 fake-indexeddb 冒充这些测试。
4. 三道现实菜谱的有经验人员审校与实际烹饪。程序里初始 reviews 为空；没有伪造审校姓名或时间。
5. AI 视觉本页直连、语音转写接入、自动文件同步、完整文化事实库 / 12 道菜、整库恢复与模板复刻。
6. 可选 WebMCP 没有受支持的浏览器验证环境；只定义一个打开视图的工具，不读取私人库存或写入，不能包装为 WorkBuddy 自动集成。

## 下一次实机验收建议

先在真实 WorkBuddy 环境加载本包，完成一遍样例演练；再用一小批真实食材 JSON 逐项确认并重开页面核对。在没有完整实机证据前，保持“本地工作版”标识，不换成“比赛最终版”。
